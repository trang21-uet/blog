export enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
}

export enum DataStatus {
  IDLE,
  LOADING,
  SUCCESS,
  FAILED,
}

export enum Breakpoints {
  XS = 0,
  SM = 600,
  MD = 900,
  LG = 1200,
  XL = 1536,
}
