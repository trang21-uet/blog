export enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
}

export enum DataStatus {
  IDLE,
  LOADING,
  SUCCESS,
  FAILED,
}

export enum Breakpoints {
  XS = "xs",
  SM = "sm",
  MD = "md",
  LG = "lg",
  XL = "xl",
}
